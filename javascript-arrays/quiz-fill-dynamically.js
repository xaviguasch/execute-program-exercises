// Write a function fillDynamically(value, length). It should construct a new array of length filled with value

const fillDynamically = (value, length) => {
  return
}

console.log(fillDynamically('d', 3)) // ['d', 'd', 'd']
console.log(fillDynamically('a', 4)) // ['a', 'a', 'a', 'a']
